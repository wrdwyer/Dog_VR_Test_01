using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
 * Holds all the exit points, so people can deside on which exit point to run to.
 * This class is not needed if you include the transforms in CrowdResponce class. It is good to have a class that can take care of exit points.
 * */
public class PanicExits : MonoBehaviour {

    public Transform[] exitPoints;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    // Functions to manipilate exit points.
}
